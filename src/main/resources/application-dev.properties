# Development Environment Configuration

# CORS Configuration - Allow localhost on any port for development
cors.allowed.origins=http://localhost:4200,http://localhost:3000,http://localhost:8080,http://localhost:8081

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/RestBil_System
spring.datasource.username=postgres
spring.datasource.password=12345

# Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
